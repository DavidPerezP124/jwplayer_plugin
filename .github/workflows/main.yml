name: Test example app build
on:
  pull_request:
    branch:
      - main
jobs:
  build_ios:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    - uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
        architecture: x64
    - run: cd jwplayer/example
    - run: flutter pub get
    - run: flutter test
    - run: flutter build ios --release --no-codesign
  build_android:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-java@v2
      with:
        distribution: 'zulu'
        java-version: '11'
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.0.5'
    - run: cd jwplayer/example && ls
    - run: flutter pub get
    - run: flutter test
    - run: flutter build apk
    - run: flutter build appbundle
  build_web:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    - uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
    - run: cd jwplayer/example
    - run: flutter pub get
    - run: flutter test
    - run: flutter build web